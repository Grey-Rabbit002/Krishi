import 'package:flutter/material.dart';

class Gyan extends StatelessWidget {
  const Gyan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Krishi Gyan")),
    );
  }
}
